{
  "name": "plugin-canned-response",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "flex-plugin pre-script-check",
    "start": "twilio flex:plugins:start",
    "deploy": "twilio flex:plugins:deploy --major --changelog 'Changes1' --description 'Description1'",
    "release": "twilio flex:plugins:release --plugin $npm_package_name@$npm_package_version --name 'Release from nmp script' --description 'The description of this Flex Plugin Configuration'"
  },
  "dependencies": {
    "react": "16.5.2",
    "react-dom": "16.5.2",
    "typescript": "^3.9.7",
    "flex-plugin-scripts": "4.7.5-beta.0"
  },
  "devDependencies": {
    "@twilio/flex-ui": "^1",
    "flex-plugin": "^3.14.2",
    "react-test-renderer": "16.5.2"
  }
}